# Improve BLE reliability around disconnect events

> Archived pull request **#184** from `albert/shepherd-launcher`, migrated to [`aarmea/lunchbox`](https://github.com/aarmea/lunchbox).

| | |
|---|---|
| Number | [#184](https://github.com/aarmea/lunchbox/issues/184) (placeholder issue in `aarmea/lunchbox`) |
| State | Merged |
| Author | `albert` |
| Source branch | `fix/ble-false-disconnect-drops-response` |
| Target branch | `main` |
| Head commit | `0fbd5f49f1a50ed08c119932c2d27866e2c95597` |
| Merge commit | `1393cfa606d316a4583abdff7606a35c102830cc` |
| Merge base | `f3cfa9c6d5712c1a0aacc38c9ede33e7111c8f9b` |
| Opened | 2026-09-07 17:38 UTC |
| Merged | 2026-09-07 17:47 UTC |
| Merged by | `albert` |
| CI status | **pass** (19 success; 19 checks) |
| Diff | 2 files changed, +349 / -14 |
| Original | [albert/shepherd-launcher!184](https://git.armeafamily.com/albert/shepherd-launcher/pulls/184) |

## Description

Found while reverifying [#177](https://github.com/aarmea/lunchbox/issues/177) on hardware. Not filed as an issue yet, so there is
no `Closes` line — happy to add one.

After a first pairing the companion's device screen sat empty, and the app
logged:

```
dispatch: failed to parse response frame (7456B): {"id":2,"result":{"api_version":1,…
```

That line is a red herring twice over. It is not a framing bug, and it is not
the app's fault.

## What is actually happening

BlueZ is not being flaky. It is being precise, and the daemon was reading it
imprecisely.

BlueZ 5.85 started with `Experimental` — which shepherd **requires**, because
the dial-out fix needs `PreferredBearer` — puts three interfaces on one device
object: `org.bluez.Device1`, `org.bluez.Bearer.LE1` and
`org.bluez.Bearer.BREDR1`. All three have a `Connected` property.

`bluer::Device::events()` throws the interface away. It carries the emitting
interface all the way to `Event::PropertiesChanged { changed, .. }`, discards it
there, and maps the payload by property *name*. So a `Connected` change on the
BR/EDR bearer arrives indistinguishable from the device's own.

A dual-mode phone brings its classic profiles up and down on its own schedule
after LE bonding. Captured on the system bus during a pairing:

```
17:25:27.685  InterfacesAdded dev_53_73_F6_74_4E_DA
              Device1  Address="B8:F4:A4:E5:20:F1"  Bonded=true  PreferredBearer="last-used"
17:25:31.609  org.bluez.Bearer.BREDR1   Connected=true      <- the phone's classic profiles
17:25:31.611  org.bluez.Device1         PreferredBearer="bredr"   <- our pin
17:25:31.861  InterfacesAdded: org.bluez.Network1, org.bluez.MediaControl1
              Device1  Modalias + UUIDs = A2DP 110a/110c/110e, AVRCP 110e,
                                          PBAP 1112, HFP 111f …   <- BR/EDR SDP
17:25:32.194  Device1  ServicesResolved=false
17:25:32.194  org.bluez.Bearer.BREDR1   Connected=false
17:25:32.194  org.bluez.Bearer.BREDR1.Disconnected  "org.bluez.Reason.Unknown"
```

`org.bluez.Device1.Connected` **never changed**. Across the whole capture every
genuine LE disconnect emitted `Bearer.LE1.Connected=false` *and*
`Device1.Connected=false` together; this event emitted only the BR/EDR pair. The
LE ACL carrying the GATT service was up the entire time, which is exactly why
the companion's RPCs kept working either side of it.

So the daemon saw a *classic-audio* bearer flap and treated it as its GATT peer
connecting and then vanishing. On the vanishing it called `reset_session()`,
which cleared both outboxes unconditionally — taking the ~7.4 KB `service_state`
the companion had just asked for and was 2048 bytes into draining.

The companion cannot see that coming: a truncated read is indistinguishable from
an idle one. It polled an empty characteristic until its 15-second RPC timeout.
Measured on a device, three times, always the same shape:

```
16:52:35.481  service_state (id=2) response queued          ~7.4 KB
16:52:35.672  read 512  ┐
16:52:35.918  read 512  │  2048 B drained
16:52:36.257  read 512  │
16:52:36.454  read 512  ┘
16:52:36.532  Connected(false) -> clear()                   <- the other ~5.4 KB
              ... 14 s of empty reads ...
16:52:50.544  list_groups (id=3)                            <- 15 s REQUEST_TIMEOUT_MS
```

Two symptoms, one cause, depending on how much the app had buffered when the
clear landed: enough to complete a frame and the reassembler pads the honest
7456-byte length out of the *next* response's bytes and logs the parse failure
above; not enough and the frame never completes, nothing is logged at all, and
the app just stalls. After a first pairing the response in question is the
opening `service_state`, so the first screen a new user sees is empty.

## It is the reverse dial-out bug's sibling

`pin_peer_to_bredr` is the fix for that one: probing an `auto_connect` profile on
a bonded phone made bluetoothd call `device_set_auto_connect(TRUE)` →
`MGMT_OP_ADD_DEVICE action=0x02`, after which *the kernel* dialled the phone, the
box took the central role, and — since only a central may start encryption —
every read of an `encrypt_authenticated` characteristic came back
`Insufficient Authentication`, forever, on a link that never dropped.

Both are the same confusion: **one bond, two bearers, and code that says "the
device" when it means "the LE link."** Dial-out was the *kernel* using the wrong
bearer; this is the *daemon* listening to the wrong one. They are even coupled
through `-E`: the experimental flag the dial-out fix needs for `PreferredBearer`
is the same flag that surfaces the per-bearer interfaces this bug rides in on. A
stock bluetoothd has neither.

Worth stating plainly because the log makes it look otherwise: **the pin is not
the cause.** The BR/EDR bearer connected 2 ms *before* the `PreferredBearer`
write, and the flap reproduces at the same 500–600 ms width with the pin in
place.

## The fix, in two layers

### 1. Read the property back instead of trusting the event

`is_connected()` asks for `Device1`'s own `Connected`, so it answers for the
device rather than for whichever bearer last twitched. One D-Bus round trip per
event turns "something about connectivity changed" into the fact.

This is the root cause, and it fixes more than the data loss: the daemon was
also arming its first-RPC watchdog and re-pinning the bearer off classic-profile
churn.

### 2. Do not discard bytes a peer is mid-read anyway

The outboxes now go through `Outbox::clear_if_aligned`, which declines while a
peer holds a partial frame. That is not a new rule — `Outbox::push_inner` and the
`Connected(true)` handler already follow it, and say why: bytes a peer is in the
middle of reading are not ours to throw away. The disconnect path was the one
place still ignoring it.

Nothing leaks. Anything genuinely stale is disposed of twice over, by the
companion's post-connect drain and by the `id == 1` clear in `handle_request`,
and the guard releases the moment the peer finishes the frame.

The request-side `FrameReader` is still reset unconditionally, and that asymmetry
is deliberate: an orphaned partial *request* has no second chance, because
nothing on this side can resync a byte stream that starts mid-frame and the next
session's writes would stitch onto the orphan. Requests arrive in a single ATT
write in practice, so there is next to nothing in flight to protect.

Layer 2 is kept as the seatbelt rather than dropped once layer 1 landed:
`Device1.Connected` can still go false for real while the companion is mid-frame
— a genuine drop during a large response — and the guard is what makes that a
disconnect rather than silent data loss. Its cost is one `if`.

## How it was verified

**Unit tests that discriminate.** Reverting `reset_session` to the unconditional
clear fails `a_reported_disconnect_spares_a_response_the_peer_is_still_reading`
and `a_reported_disconnect_always_drops_a_half_written_request`, while
`reset_session_wipes_all_session_state` and
`a_reported_disconnect_clears_an_outbox_no_one_is_mid_frame_on` keep passing.
That last one is the anti-leak guard: an outbox nobody is mid-frame on is still
cleared.

**Both layers caught in the act on a device**, with `RUST_LOG='info,shepherd_ble=debug'`
and a `dbus-monitor` capture of `org.bluez` running alongside:

- *Layer 2.* BR/EDR bearer up at 17:25:31.609, pin, down at 17:25:32.194 — and
  the daemon logged `peer reported gone mid-delivery; keeping what it is still
  reading outbox="response"`. The companion went from `service_state` to its next
  RPC in **4.1 s** (the natural drain) instead of the 15 s timeout, and the
  device screen populated immediately.
- *Layer 1.* Same flap, same width (17:32:08.576 true → 17:32:09.099 false,
  523 ms), `Device1.Connected` unchanged — and the disconnect arm did not run at
  all. The connect arm still ran once, correctly, because the device really was
  connected.

**Full BLE regression pass on the phone** either side of the change: factory
reset → re-pair (Numeric Comparison, bond `LE:Y` with `EncryptionStatus{keySize=16`)
→ `claim` → all management reads (`service_state`, `list_groups`,
`list_diagnostics`, `get_volume`, `get_brightness`, `list_audio_outputs`) → a
management write (`set_mute`, which reached the launcher and HUD as
`VolumeChanged { muted: true }`) → reconnect on the existing bond with
`BLE outbox backlog drained bytes=10684 reads=22`.

`cargo test --workspace --all-targets` (68 binaries), `cargo clippy --workspace
--all-targets -- -D warnings`, `cargo fmt --all` — all clean.

## Notes for review

- **Upstream bug worth reporting.** `bluer::Device::events()` has the emitting
  interface in hand and drops it (`device.rs`, `Event::PropertiesChanged {
  changed, .. }`). Any bluer consumer on BlueZ ≥ 5.82 with `Experimental` will
  see bearer property changes as device ones. The read-back works around it
  without waiting on upstream; a bluer fix would let this become a filter.
- **The cost of layer 1** is one D-Bus round trip per `Connected` event on a
  device object. These are rare — connect, disconnect, and bearer churn — so it
  is not on any hot path.
- **The connect arm now fires on a bearer event that reads back as connected.**
  That is intended and is arguably a fix in itself: a device object created
  already-connected emits no `Device1.Connected=true` transition at all (the
  initial value arrives in `InterfacesAdded`), so the first bearer event is
  sometimes the only chance to notice. It is deduplicated, so it runs once.
- **Unrelated flake seen once**, recorded in the history note: `shepherd-host-linux`'s
  lib tests failed once (151 passed, 1 failed, name not captured) while a
  headless dev session was live on the box, then passed six consecutive runs and
  the full workspace. This change touches only `shepherd-ble`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01PQup42jkA5PTiyDXEAN7a5

## CI checks

<details>
<summary>19 checks on <code>0fbd5f49f1a5</code></summary>

| Check | Result | Detail |
|---|---|---|
| CI / Android companion (unit tests) (pull_request) | pass | [Successful in 51s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/427/jobs/11) |
| CI / Android media (cargo-ndk build) (pull_request) | pass | [Successful in 49s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/427/jobs/12) |
| CI / Arch neutrality (pull_request) | pass | [Successful in 9s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/427/jobs/15) |
| CI / Build (pull_request) | pass | [Successful in 4m1s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/427/jobs/2) |
| CI / CI image (Android) (pull_request) | pass | [Successful in 24s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/427/jobs/18) |
| CI / CI image (pull_request) | pass | [Successful in 25s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/427/jobs/17) |
| CI / CI images (pull_request) | pass | [Successful in 0s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/427/jobs/0) |
| CI / Clippy (pull_request) | pass | [Successful in 2m14s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/427/jobs/5) |
| CI / Config editor (pull_request) | pass | [Successful in 1m42s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/427/jobs/13) |
| CI / E2E (pull_request) | pass | [Successful in 4m43s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/427/jobs/4) |
| CI / Firewall E2E (pull_request) | pass | [Successful in 5m26s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/427/jobs/10) |
| CI / Package (.deb smoke build) (pull_request) | pass | [Successful in 3m55s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/427/jobs/8) |
| CI / Rustfmt (pull_request) | pass | [Successful in 10s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/427/jobs/6) |
| CI / ShellCheck (pull_request) | pass | [Successful in 15s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/427/jobs/14) |
| CI / Test (pull_request) | pass | [Successful in 4m44s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/427/jobs/3) |
| CI / Version harmony (pull_request) | pass | [Successful in 8s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/427/jobs/9) |
| CI / Warm cargo registry (pull_request) | pass | [Successful in 26s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/427/jobs/1) |
| CI / Web UI (pull_request) | pass | [Successful in 28s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/427/jobs/7) |
| CI / Workflow syntax (pull_request) | pass | [Successful in 9s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/427/jobs/16) |

</details>

## Discussion

_1 comment archived from Forgejo._

### `albert` — 2026-09-07 17:47 UTC

Code looks reasonable and I'm satisfied with Claude's automated testing that exercised the full BLE flow

---

[Back to the index](../README.md)
