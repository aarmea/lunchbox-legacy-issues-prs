# feat(admin): install the companion and media apps from the admin script

> Archived pull request **#131** from `albert/shepherd-launcher`, migrated to [`aarmea/lunchbox`](https://github.com/aarmea/lunchbox).

| | |
|---|---|
| Number | [#131](https://github.com/aarmea/lunchbox/issues/131) (placeholder issue in `aarmea/lunchbox`) |
| State | Merged |
| Author | `albert` |
| Source branch | `feat/admin-installs-android-apps` |
| Target branch | `main` |
| Head commit | `6c0a3422bba97418682b340df57369e56f9c641d` |
| Merge commit | `089262181f22b660fbb7180458275a644b6b333c` |
| Merge base | `e79d34a4f94988a825e55ce179d822a29877a1ac` |
| Opened | 2026-08-17 22:59 UTC |
| Merged | 2026-08-17 23:26 UTC |
| Merged by | `albert` |
| CI status | **pass** (16 success; 16 checks) |
| Diff | 9 files changed, +563 / -8 |
| Original | [albert/shepherd-launcher!131](https://git.armeafamily.com/albert/shepherd-launcher/pulls/131) |

## Description

`shepherd-admin apps install companion|media` fetches an APK and pushes it
onto an attached Android device over adb. Where the APK comes from follows
how shepherd itself was installed, which is the point: a source checkout
builds the app's own Gradle project, so you install what you just wrote,
while a packaged install downloads the release asset matching its VERSION
from Forgejo and verifies it against the .sha256 sidecar release.yml
uploads beside every asset. `--source` / `--release` override the guess;
`--version` and `--apk` cover the rest.

It lives in scripts/lib/admin.sh so both entrypoints get it — `shepherd`
from a checkout, `shepherd-admin` from the .deb — which is what makes the
source/packaged split expressible in one place.

The details that matter:

* No root. adb authorises devices against the *invoking user's* key, so a
  sudo run turns an authorised phone into `unauthorized`, and a root Gradle
  run leaves root-owned build output in the checkout. The command refuses
  sudo and says why.
* The device is resolved before anything is built or downloaded — a missing
  phone shouldn't cost a Gradle run — and a HOST:PORT serial is connected
  first, which is how the Fire TV sticks are reached.
* An unverifiable download is fatal, not a warning: this file is about to
  be installed on a family device. Cached APKs are re-verified before
  reuse, so a truncated download self-heals.
* INSTALL_FAILED_UPDATE_INCOMPATIBLE (a debug build meeting a release- or
  F-Droid-signed copy) is translated into the `adb uninstall` it needs —
  with a warning, for the companion app, that this erases the admin records
  and claim tokens for every device that phone administers.
* Gradle's output goes to stderr: the function's stdout is the APK path,
  and a build log mixed into it becomes the filename.

adb and curl join the .deb as Suggests rather than Depends — a kiosk that
never has a phone plugged into it shouldn't carry the Android platform
tools — and both are checked at call time with the apt line that fixes it.

Verified against the real release server and a phone on USB: the source
build for both apps (including the media app's cargo-ndk cross-compile),
the release download for 0.3.5 and 0.3.6, and a packaged layout simulated
under a fake /usr/lib/shepherd, which downloaded, checksum-verified, and
installed shepherd-media 0.3.6 — release-signed with the certificate
dist/fdroid pins, launching cleanly on the device. Failure paths exercised
too: 404, corrupted cache, unknown serial, sudo, missing adb/curl.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_014RTqj36qW8Vax6GZ86pBAo

## CI checks

<details>
<summary>16 checks on <code>6c0a3422bba9</code></summary>

| Check | Result | Detail |
|---|---|---|
| CI / Android companion (unit tests) (pull_request) | pass | [Successful in 1m59s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/280/jobs/10) |
| CI / Android media (cargo-ndk build) (pull_request) | pass | [Successful in 44s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/280/jobs/11) |
| CI / Arch neutrality (pull_request) | pass | [Successful in 13s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/280/jobs/13) |
| CI / Build (pull_request) | pass | [Successful in 2m44s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/280/jobs/2) |
| CI / CI image (Android) (pull_request) | pass | [Successful in 22s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/280/jobs/15) |
| CI / CI image (pull_request) | pass | [Successful in 23s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/280/jobs/14) |
| CI / CI images (pull_request) | pass | [Successful in 0s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/280/jobs/0) |
| CI / Clippy (pull_request) | pass | [Successful in 1m22s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/280/jobs/5) |
| CI / E2E (pull_request) | pass | [Successful in 2m53s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/280/jobs/4) |
| CI / Firewall E2E (pull_request) | pass | [Successful in 3m51s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/280/jobs/9) |
| CI / Package (.deb smoke build) (pull_request) | pass | [Successful in 2m5s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/280/jobs/7) |
| CI / Rustfmt (pull_request) | pass | [Successful in 25s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/280/jobs/6) |
| CI / ShellCheck (pull_request) | pass | [Successful in 17s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/280/jobs/12) |
| CI / Test (pull_request) | pass | [Successful in 2m50s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/280/jobs/3) |
| CI / Version harmony (pull_request) | pass | [Successful in 13s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/280/jobs/8) |
| CI / Warm cargo registry (pull_request) | pass | [Successful in 35s](https://git.armeafamily.com/albert/shepherd-launcher/actions/runs/280/jobs/1) |

</details>

---

[Back to the index](../README.md)
